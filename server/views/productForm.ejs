<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <title>
    <%= product ? "Editar Producto" : "Agregar Producto" %>
  </title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="/favicon.png" />
</head>

<body class="bg-dark text-white">

  <nav class="navbar bg-dark navbar-expand-lg fixed-top custom-navbar text-white shadow-sm py-2">
    <div class="container-fluid">
      <div class="d-flex align-items-center flex-grow-1">
        <a class="navbar-brand d-flex align-items-center text-white me-3" href="/admin/dashboard">
          <img src="/logo.png" alt="Logo" width="40" height="40" class="me-2" />
          <span class="fs-5">TechStore</span>
        </a>
        <span class="navbar-text text-white small">Panel Administrador</span>
      </div>
      <div>
        <a href="/admin/dashboard" class="btn btn-outline-light">Volver al Dashboard</a>
      </div>
    </div>
  </nav>

  <!-- Contenido principal -->
  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card p-4 shadow bg-secondary w-100" style="max-width: 500px;">
      <h2 class="text-center mb-4 text-white">
        <%= product ? "Editar Producto" : "Agregar Nuevo Producto" %>
      </h2>

      <form action="<%= product ? '/admin/products/' + product.id + '/edit' : '/admin/products/new' %>" method="POST"
        enctype="multipart/form-data" class="d-grid gap-3">

        <div>
          <label for="nombre" class="form-label text-white">Nombre:</label>
          <input type="text" id="nombre" name="nombre" class="form-control" value="<%= product ? product.nombre : '' %>"
            required>
        </div>

        <div>
          <label for="precio" class="form-label text-white">Precio:</label>
          <input type="number" id="precio" name="precio" step="0.01" min="1" class="form-control"
            value="<%= product ? product.precio : '' %>" required>
        </div>

        <div>
          <label for="tipo" class="form-label text-white">Tipo:</label>
          <select id="tipo" name="tipo" class="form-select" required>
            <option value="">-- Selecciona un tipo --</option>
            <option value="notebook" <%=product && product.tipo==='notebook' ? 'selected' : '' %>>Notebook</option>
            <option value="pc" <%=product && product.tipo==='pc' ? 'selected' : '' %>>PC</option>
          </select>
        </div>

        <div>
          <label for="imagen" class="form-label text-white">Imagen:</label>
          <input type="file" id="imagen" name="imagen" class="form-control" <%=product ? '' : 'required' %>>
        </div>

        <button type="submit" class="btn btn-light w-100">
          <%= product ? "Guardar Cambios" : "Agregar Producto" %>
        </button>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>