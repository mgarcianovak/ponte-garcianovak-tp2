<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= product ? "Editar Producto" : "Agregar Producto" %></title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 600px; margin: 40px auto; }
    form { display: flex; flex-direction: column; gap: 10px; }
    label { font-weight: bold; }
    input, select { padding: 8px; font-size: 1rem; }
    button { padding: 10px; cursor: pointer; }
  </style>
</head>
<body>

  <h1><%= product ? "Editar Producto" : "Agregar Nuevo Producto" %></h1>

  <form 
    action="<%= product ? '/admin/products/' + product.id + '/edit' : '/admin/products/new' %>" 
    method="POST" 
    enctype="multipart/form-data">

    <label for="nombre">Nombre:</label>
    <input type="text" id="nombre" name="nombre" value="<%= product ? product.nombre : '' %>" required>

    <label for="precio">Precio:</label>
    <input type="number" id="precio" name="precio" step="0.01" value="<%= product ? product.precio : '' %>" required>

    <label for="tipo">Tipo:</label>
    <select id="tipo" name="tipo" required>
      <option value="">-- Selecciona un tipo --</option>
      <option value="notebook" <%= product && product.tipo === 'notebook' ? 'selected' : '' %>>Notebook</option>
      <option value="pc" <%= product && product.tipo === 'pc' ? 'selected' : '' %>>PC</option>
    </select>

    <label for="imagen">Imagen:</label>
    <input type="file" id="imagen" name="imagen" <%= product ? '' : 'required' %>>

    <button type="submit"><%= product ? "Guardar Cambios" : "Agregar Producto" %></button>
  </form>

  <p><a href="/admin/dashboard">Volver al Dashboard</a></p>

</body>
</html>